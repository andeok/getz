spring:
  application:
    name: backend

  data:
    redis:
        host: localhost
        port: 6379

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/getz
    username: root
    password: getzpassword
    driver-class-name: com.mysql.cj.jdbc.Driver